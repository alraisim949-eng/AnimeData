pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.name = "Anime Pro Mix"

// These lines are critical: They must match the folders in your project
include(":mobile")
include(":tv")